# === OS ===
.DS_Store
**/.DS_Store

# === Editor / IDE ===
.vscode-test
.history/

# === Secrets / Environment ===
.env
.env.local
.env.*.local

# === Dotfiles: machine-specific configs ===
common/configstore/
common/github-copilot/
mac/raycast/.config/raycast/config.json
mac/raycast/.config/raycast/extensions/

# gh CLI (auth tokens are machine-specific)
common/gh/.config/gh/hosts.yml

# Machine-specific configs (contains local paths)
common/uv/.config/uv/uv-receipt.json
common/org.qgroundcontrol/

# Local zsh config (machine-specific)
mac/zsh/.zshrc.local
linux/zsh/.zshrc.local
