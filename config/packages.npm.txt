# NPM packages to install globally (shared across all platforms)

# AI CLI tools
@anthropic-ai/claude-code
@openai/codex
@google/gemini-cli

# Claude Code token usage analytics (local JSONL parser)
ccusage

# Neovim dependencies
@mermaid-js/mermaid-cli
@softwaretechnik/dbml-renderer
