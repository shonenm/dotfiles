{
  "mcpServers": {
    "tmux": {
      "type": "stdio", "command": "npx", "args": ["-y", "tmux-mcp"],
      "description": "", "permission": "allow", "enabled": false
    },
    "serena": {
      "type": "stdio", "command": "serena", "args": ["start-mcp-server", "--context", "claude-code", "--project-from-cwd"],
      "description": "", "permission": "allow", "enabled": false
    },
    "token-optimizer": {
      "type": "stdio", "command": "npx", "args": ["-y", "token-optimizer-mcp"],
      "description": "", "permission": "allow", "enabled": false
    }
  }
}
