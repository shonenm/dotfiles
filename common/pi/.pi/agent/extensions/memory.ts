// Persistent memory is provided by the pi-hermes-memory package.
// Keep this no-op compatibility stub so existing stow links do not break on upgrade.
import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";

export default function (_pi: ExtensionAPI) {}
