# live-pr global config (stowed to ~/.config/live-pr/config.toml).
# Overlaid by any per-repo .live-pr.toml.

[diff]
# Conversation (left) : Review (right) = 2 : 8.
split_ratio = 20
# Floor for either pane so the narrow conversation column stays usable.
min_pane_width = 30
