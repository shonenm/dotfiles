# --- Prefix Key ---
set -g prefix C-j
unbind C-b
bind C-j send-prefix

# --- General ---
set -g mouse on

# Terminal title (for aerospace window detection)
set -g set-titles on
set -g set-titles-string "#{b:pane_current_path}"

# --- Copy Mode (vi-style) ---
setw -g mode-keys vi
bind-key -T copy-mode-vi 'v' send -X begin-selection
bind-key -T copy-mode-vi 'C-v' send -X rectangle-toggle
bind-key -T copy-mode-vi 'y' send -X copy-selection
bind-key -T copy-mode-vi Enter send-keys -X cancel
bind-key -T copy-mode-vi Escape send-keys -X cancel
# スクロール量を4行に変更（デフォルトは半ページ）
bind-key -T copy-mode-vi C-u send-keys -X scroll-up \; send-keys -X scroll-up \; send-keys -X scroll-up \; send-keys -X scroll-up
bind-key -T copy-mode-vi C-d send-keys -X scroll-down \; send-keys -X scroll-down \; send-keys -X scroll-down \; send-keys -X scroll-down
# Mouse drag: auto-copy to clipboard, exit copy-mode and scroll to bottom
bind-key -T copy-mode-vi MouseDragEnd1Pane send-keys -X copy-pipe-and-cancel "pbcopy"
set -g base-index 1
setw -g pane-base-index 1
set -s escape-time 0

# Activity monitoring
setw -g monitor-activity on
set -g renumber-windows on

# --- Terminal / Color (Ghostty + Neovim) ---
set -g default-terminal "${TERM}"
set -ag terminal-features ",xterm-256color:RGB"
# Undercurl support
set -as terminal-overrides ',*:Smulx=\E[4::%p1%dm'
set -as terminal-overrides ',*:Setulc=\E[58::2::%p1%{65536}%/%d::%p1%{256}%/%{255}%&%d::%p1%{255}%&%d%;m'

# --- Keybindings (Vim-like) ---
# Pane split
bind | split-window -h -c "#{pane_current_path}"
bind - split-window -v -c "#{pane_current_path}"
unbind '"'
unbind %

# Pane navigation
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# MOVE mode (prefix + m で入り、Enter/Esc で抜ける)
bind m switch-client -T move
bind -T move h select-pane -L \; switch-client -T move
bind -T move j select-pane -D \; switch-client -T move
bind -T move k select-pane -U \; switch-client -T move
bind -T move l select-pane -R \; switch-client -T move
bind -T move C-h select-window -t :- \; switch-client -T move
bind -T move C-l select-window -t :+ \; switch-client -T move
bind -T move Enter switch-client -T root
bind -T move Escape switch-client -T root
# 無効キーはモード継続（誤操作防止）
bind -T move a switch-client -T move
bind -T move b switch-client -T move
bind -T move c switch-client -T move
bind -T move d switch-client -T move
bind -T move e switch-client -T move
bind -T move f switch-client -T move
bind -T move g switch-client -T move
bind -T move i switch-client -T move
bind -T move m switch-client -T move
bind -T move n switch-client -T move
bind -T move o switch-client -T move
bind -T move p switch-client -T move
bind -T move q switch-client -T move
bind -T move r switch-client -T move
bind -T move s switch-client -T move
bind -T move t switch-client -T move
bind -T move u switch-client -T move
bind -T move v switch-client -T move
bind -T move w switch-client -T move
bind -T move x switch-client -T move
bind -T move y switch-client -T move
bind -T move z switch-client -T move
bind -T move Space switch-client -T move
bind -T move 0 switch-client -T move
bind -T move 1 switch-client -T move
bind -T move 2 switch-client -T move
bind -T move 3 switch-client -T move
bind -T move 4 switch-client -T move
bind -T move 5 switch-client -T move
bind -T move 6 switch-client -T move
bind -T move 7 switch-client -T move
bind -T move 8 switch-client -T move
bind -T move 9 switch-client -T move

# Pane resize
bind -r H resize-pane -L 5
bind -r J resize-pane -D 5
bind -r K resize-pane -U 5
bind -r L resize-pane -R 5

# Reload config
bind r source-file ~/.config/tmux/tmux.conf \; display-message -d 800 "#[fg=#9ece6a]  Config Reloaded!"

# Pane title
bind t command-prompt -p "Pane title:" "select-pane -T '%%'"

# Window title
bind T command-prompt -p "Window name:" "rename-window '%%'"

# Window navigation
bind -r C-h select-window -t :-
bind -r C-l select-window -t :+

# Sync mode toggle (with style change)
bind S run-shell '\
  if [ "$(tmux show-window-options -v synchronize-panes)" = "on" ]; then \
    tmux setw synchronize-panes off; \
    tmux set -g window-style "fg=colour244,bg=default"; \
    tmux set -g pane-border-style "fg=#3b4261,bg=default"; \
  else \
    tmux setw synchronize-panes on; \
    tmux set -g window-style "fg=colour255,bg=default"; \
    tmux set -g pane-border-style "fg=#73daca,bg=default"; \
  fi'

# --- Status Bar ---
set -g status-position top

# TokyoNight theme
source-file ~/.config/tmux/tokyonight.tmux

# Claude Code 通知統合
source-file ~/.config/tmux/claude-hooks.tmux

# --- Plugins (TPM) ---
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-yank'
set -g @plugin 'tmux-plugins/tmux-continuum'

# Plugin settings
set -g @continuum-restore 'on'
set -g @resurrect-capture-pane-contents 'on'
set -g @resurrect-strategy-nvim 'session'

# Initialize TPM (keep at the bottom)
setenv -g TMUX_PLUGIN_MANAGER_PATH "$HOME/.tmux/plugins/"
if "test ! -d ~/.tmux/plugins/tpm" \
   "run 'git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm && ~/.tmux/plugins/tpm/bin/install_plugins'"
run -b '~/.tmux/plugins/tpm/tpm'

# Force transparent background (Tmux 3.2+ requires both)
set -g status-bg default
set -g status-style bg=default

# --- Visual Settings (Transparency Best Practice) ---

# 1. 透過を維持しつつ、非アクティブペインの文字を暗くする
set -g window-style 'fg=colour244,bg=default'
set -g window-active-style 'fg=colour255,bg=default'

# ペインボーダー（二重線で視認性向上）
set -g pane-border-lines double
set -g pane-border-style 'fg=#3b4261,bg=default'
# copy mode: 赤枠, move: マゼンタ枠, sync: ティール枠, prefix: 黄枠, 通常: 青枠
set -g pane-active-border-style '#{?pane_in_mode,fg=#f7768e bg=#1a1b26,#{?#{==:#{client_key_table},move},fg=#bb9af7 bg=#1a1b26,#{?pane_synchronized,fg=#73daca bg=#1a1b26,#{?client_prefix,fg=#e0af68 bg=#1a1b26,fg=#7aa2f7 bg=#1a1b26}}}}'
set -g pane-border-status top
set -g pane-border-format " #P: #{?pane_title,#{pane_title},#{pane_current_command}} "
set -g pane-border-indicators both
